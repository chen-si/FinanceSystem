<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.heu.finance.mapper.normal.finance.UserFundProductMapper">

    <insert id="addUserFundProduct">
        insert into user_fund_product(userId, fundId, averYield, profit, status)
        values (#{userId},#{fundId},#{averYield},#{profit},#{status})
    </insert>
    <update id="updateUserFundProductStatus">
        update user_fund_product set status = #{status}
        where id = #{id}
    </update>
    <delete id="deleteFundProductById">
        delete from user_fund_product
        where id = #{id}
    </delete>
</mapper>